project_name: rox_orchestrator

builds:
  - id: rox_orchestrator
    main: ./cmd/rox_orchestrator
    binary: rox_orchestrator
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: rox_orchestrator
    ids: [rox_orchestrator]
    formats: [tar.gz]
    name_template: "rox_orchestrator_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: touchmeangel
    name: rox_orchestrator