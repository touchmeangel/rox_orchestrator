### Usage
```
ignite
ignite -h or --help
ignite -u                          (Pull latest image and run in current directory)
ignite /path/to/project            (Explicit path)
ignite . -r or --reconfigure
```

### Install
```
pip install git+https://github.com/touchmeangel/ignite_ux
```